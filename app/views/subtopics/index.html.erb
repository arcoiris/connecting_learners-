<div class="page"><h2>Your Topic Search Results:</h2>

<% if @subtopics %>
	<% @subtopics.each do |subtopic| %>
		<p><%= link_to "#{subtopic.name}", subtopic_path(subtopic.id) %></p><hr>
	<% end %>
<% end %> 
</div>